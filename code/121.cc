#include <iostream>
#include <vector>

int maxprice(std::vector<int>& vec) {
    if(vec.empty()) return 0;
    int mx = 0, k = vec[0];
    for (auto& itr : vec) {
        
    }

}


int main() {
    return 0;
}